services:

  postgres:

    image: postgres:17

    container_name: spotinder-postgres

    environment:
      POSTGRES_DB: spotinder
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: